In questo caso creiamo un container `mysql` e uno `phpmyadmin`. 

L'immagine mysql conterrà già dei dati grazie agli SQL dentro `/init`, importati come "entrypoint"

Credenziali phpmyadmin: `root - mysqlroot`